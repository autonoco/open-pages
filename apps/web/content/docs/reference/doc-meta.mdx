---
title: Doc module exports
description: The shape the runtime expects from each docs/<id>/index.tsx.
---

```tsx title="docs/<id>/index.tsx"
import type { Page, DocMeta } from '@open-pdf/core';

const Cover: Page = () => <div>{/* ... */}</div>;

export const meta: DocMeta = {
  title: 'Cover',
  theme: 'corporate',
};

export const notes = ['Open with the analyst quote.'];

export default [Cover] satisfies Page[];
```

## Default export

`Page[]` — the array of components, in display order. Each component fills
its container; the runtime renders one at a time onto the 1920×1080 canvas.

```ts
type Page = ComponentType & {
  transition?: DocTransition;
};
```

## `meta` (optional)

<TypeTable
  type={{
    title: {
      type: 'string',
      description: 'Display title for the deck — used in the doc browser.',
    },
    theme: {
      type: 'string',
      description: 'Name of a theme under themes/<id>.md. The agent reads it before authoring.',
    },
    createdAt: {
      type: 'string',
      description: 'ISO 8601 timestamp set at scaffold time. Sorts the doc list.',
    },
  }}
/>

## `notes` (optional)

Plain-text strings (line breaks preserved), index-aligned with the page
array. Surfaced in the presenter view — see
[Present mode](/docs/core-feature/present-mode).

```ts
export const notes: (string | undefined)[] = [
  'Open with a smile.',
  undefined,
  'Pause for questions.',
];
```

## `transition` (optional)

A module-level `DocTransition` becomes the default animation between every
page in the deck. Per-page overrides are assigned on the `Page` component
itself. See [DocTransition](/docs/reference/doc-transitions) for the full
schema.

```ts
import type { DocTransition } from '@open-pdf/core';

export const transition: DocTransition = {
  duration: 200,
  exit:  { /* … */ },
  enter: { /* … */ },
};
```

## Imports from `@open-pdf/core`

```ts
import {
  CANVAS_WIDTH,        // 1920
  CANVAS_HEIGHT,       // 1080
  ImagePlaceholder,    // sized empty placeholder — see /docs/reference/image-placeholder
  MorphElement,        // match an element across pages — see /docs/primitive/morph-element
} from '@open-pdf/core';
import type {
  MorphElementProps,
  Page,
  DocMeta,
  DocModule,
  DocTransition,     // per-page / module-level animations — see /docs/reference/doc-transitions
  MorphTransition,
  TransitionPhase,
  ImagePlaceholderProps,
} from '@open-pdf/core';
```
