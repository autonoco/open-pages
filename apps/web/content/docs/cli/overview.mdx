---
title: Overview
description: Two CLIs — one to scaffold, one to run.
---

open-pdf ships two CLIs:

- **`@open-pdf/cli`** — the scaffolder. One command (`init`) to bootstrap
  a new workspace.
- **`@open-pdf/core`** — the runtime CLI. `dev`, `build`, `preview`, and
  `sync:skills` for an existing workspace.

After `init`, `package.json` exposes the runtime CLI under standard scripts:

```json
{
  "scripts": {
    "dev": "open-pdf dev",
    "build": "open-pdf build",
    "preview": "open-pdf preview",
    "sync:skills": "open-pdf sync:skills"
  }
}
```

So in day-to-day use you'll just type `npm run dev`, `npm run build`, etc.
