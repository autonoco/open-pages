---
title: /current-doc
description: Resolves "this doc" / "this element" to a concrete reference.
---

`/current-doc` is what makes deictic prompts work. *"This page"*, *"this
heading"*, *"the doc I'm on"* — the skill teaches the agent how to
resolve them to a concrete doc id, page index, and (when set) the JSX
element you've picked in the [inspector](/docs/core-feature/inspector).

## How it works

The dev server writes the user's current view to
`node_modules/.open-pdf/current.json` on every navigation and selection
change:

- `docId` / `pageIndex` — the deck folder name and the active page.
- `pagePath` — the source file of that page.
- `selection` — line/column, tag name, and a text snippet of the JSX node
  picked in the inspector (`null` when nothing is picked).

The skill reads that file before asking *which doc?*

## When it fires

You don't usually invoke it directly. `/doc-authoring` cross-references
it, so any "fix this" prompt routes through it automatically.

It collapses the most common round-trip in agent-driven editing:

```text
You: make this title smaller
Agent: which title?
You: the one on doc 3
```

With `/current-doc`, the agent already knows. See
[`/apply-comments`](/docs/skills/apply-comments) for how this folds into the
broader inspector loop.
